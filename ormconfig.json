{
  "type": "mysql",
  "host": "localhost",
  "port": 3306,
  "username": "chillted",
  "password": "password",
  "database": "graph",
  "synchronize": false,
  "logging": false,
  "entities": [
     "src/models/**/*.ts"
  ],
  "migrations": [
     "src/database/migrations/**/*.ts"
  ],
  "subscribers": [
     "src/subscriber/**/*.ts"
  ],
  "cli": {
   "migrationsDir": "src/database/migrations"
  }
}